[package]
name = "sdb_api"
version = "0.1.0"
edition = "2024"
license = "MIT"

[dependencies]
hack = { version = "0.1", path = "../hack" }
hex = "0.4"
spacetimedb-sdk = "1.10.*"

[build-dependencies]
tempfile = "3"
